export const commonMixin = {
    // methods: {
    //     getDataFromResponse({ response, CODE=20000, FIELD }) {
    //         if (response.code === CODE) {
    //             return response[FIELD]
    //         }
    //         else {
    //             throw new Error('读取数据错误')
    //         }
    //     }
    // }
}